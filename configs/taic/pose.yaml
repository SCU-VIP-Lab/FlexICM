root: "logs"
exp_name: "taic_pose"
task: "pose"
dataset_path: "/home/zbellay/hdd2/Tianma/dataset/coco2017"
base_codec: "./checkpoints/base_codec/base_codec_4.pth.tar"
freeze_base_codec: false
checkpoint: null # "/media/tianma/FlexICM/logs/taic_pose/4/checkpoint_best_loss.pth.tar"
epochs: 999
learning_rate: 1.0e-4
gpu_id: 0
quality_level: 4
lmbda: 100.35
# false = train with task feature loss only (loss = λD); bpp is still logged
use_bpp_loss: true
num_workers: 4
batch_size: 4
test_batch_size: 1
patch_size: 512
# Cap long edge after short-edge expand (None / omit = no cap). Helps avoid OOM on wide images.
max_long_side: 640
cuda: true
save: true
seed: 42
pretrained_backbone: true
out_channels: 32
align_mode: "stages"

# Official task-network teacher (backbone/neck) for feature alignment D
use_official_teacher: true
task_config: "./checkpoints/task_networks/pose/higherhrnet_w32_coco_512x512.py"
task_checkpoint: "./checkpoints/task_networks/pose/higher_hrnet32_coco_512x512-8ae85183_20200713_mmpose1.pth"
